PPC64ThreadContext#getNumRegisters()::: c64 thread context get num registers:::return
PPC64ThreadContext#getRegisterName(int)::: c64 thread context get register name:::return
PPC64ThreadContext#setRegister(int, long)::: c64 thread context set register:::
PPC64ThreadContext#getRegister(int)::: c64 thread context get register:::return
PPC64ThreadContext#getTopFrame(Debugger)::: c64 thread context get top frame:::return
PPC64ThreadContext#setRegisterAsAddress(int, Address):::This can't be implemented in this class since we would have to tie the implementation to, for example, the debugging system:::
PPC64ThreadContext#getRegisterAsAddress(int):::This can't be implemented in this class since we would have to tie the implementation to, for example, the debugging system:::
