X86ThreadContext#getNumRegisters()::: x86 thread context get num registers:::return
X86ThreadContext#getRegisterName(int)::: x86 thread context get register name:::return
X86ThreadContext#setRegister(int, long)::: x86 thread context set register:::
X86ThreadContext#getRegister(int)::: x86 thread context get register:::return
X86ThreadContext#getTopFrame(Debugger)::: x86 thread context get top frame:::return
X86ThreadContext#setRegisterAsAddress(int, Address):::This can't be implemented in this class since we would have to       tie the implementation to, for example, the debugging system:::
X86ThreadContext#getRegisterAsAddress(int):::This can't be implemented in this class since we would have to       tie the implementation to, for example, the debugging system:::
