InspectedFrame#getLocal(int):::Returns the value of the object local at index:::
InspectedFrame#isVirtual(int):::Returns whether the local at index is a virtual object, and therefore the object returned by #getLocal(int) is a copy.:::
InspectedFrame#hasVirtualObjects():::Returns true if the stack frame is a compiled stack frame and there are virtual objects anywhere in the current state of the compiled method:::
InspectedFrame#materializeVirtualObjects(boolean):::This method will materialize all virtual objects, deoptimize the stack frame and make sure that subsequent execution of the deoptimized frame uses the materialized values.:::
InspectedFrame#isMethod(ResolvedJavaMethod):::Checks if the current method is equal to the given method:::
StackIntrospection#iterateFrames(ResolvedJavaMethod[], ResolvedJavaMethod[], int, InspectedFrameVisitor):::Walks the current stack, providing InspectedFrames to the visitor that can be used to inspect the stack frame's contents:::
