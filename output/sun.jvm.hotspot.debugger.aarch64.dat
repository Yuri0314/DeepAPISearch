AARCH64ThreadContext#getNumRegisters()::: h64 thread context get num registers:::return
AARCH64ThreadContext#getRegisterName(int)::: h64 thread context get register name:::switch->return->return->return->return
AARCH64ThreadContext#setRegister(int, long)::: h64 thread context set register:::
AARCH64ThreadContext#getRegister(int)::: h64 thread context get register:::return
AARCH64ThreadContext#getTopFrame(Debugger)::: h64 thread context get top frame:::return
AARCH64ThreadContext#setRegisterAsAddress(int, Address):::This can't be implemented in this class since we would have to tie the implementation to, for example, the debugging system:::
AARCH64ThreadContext#getRegisterAsAddress(int):::This can't be implemented in this class since we would have to tie the implementation to, for example, the debugging system:::
