AARCH64ThreadContext#setRegisterAsAddress(int, Address):::This can't be implemented in this class since we would have to tie the implementation to, for example, the debugging system:::
AARCH64ThreadContext#getRegisterAsAddress(int):::This can't be implemented in this class since we would have to tie the implementation to, for example, the debugging system:::
