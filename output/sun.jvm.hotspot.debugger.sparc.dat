SPARCThreadContext#getNumRegisters()::: thread context get num registers:::return
SPARCThreadContext#getRegisterName(int)::: thread context get register name:::return
SPARCThreadContext#setRegister(int, long)::: thread context set register:::
SPARCThreadContext#getRegister(int)::: thread context get register:::return
SPARCThreadContext#getTopFrame(Debugger)::: thread context get top frame:::return
SPARCThreadContext#setRegisterAsAddress(int, Address):::This can't be implemented in this class since we would have to       tie the implementation to, for example, the debugging system:::
SPARCThreadContext#getRegisterAsAddress(int):::This can't be implemented in this class since we would have to       tie the implementation to, for example, the debugging system:::
